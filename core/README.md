# Core

Runtime-independent domain logic.

This area owns canonical scene semantics, revisions, typed design commands, lock validation and shared value objects.

**Must not depend on:** Blender, ComfyUI, a specific LLM server, browser APIs or provider-specific payloads.
