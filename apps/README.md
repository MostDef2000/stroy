# Applications

Planned applications:

- `api/` - FastAPI control plane and project API;
- `web/` - browser project/editor experience.

Applications orchestrate core domain services and adapters; they do not redefine scene semantics.
