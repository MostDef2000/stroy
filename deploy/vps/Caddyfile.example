stroy.mostdef.ru {
    encode zstd gzip

    # The final internal app address may change when the web/API container layout lands.
    reverse_proxy 127.0.0.1:8000

    header {
        X-Content-Type-Options nosniff
        Referrer-Policy strict-origin-when-cross-origin
    }
}
